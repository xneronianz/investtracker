name: Update NAV Data

on:
  schedule:
    - cron: '0 1 * * 1-5'
  workflow_dispatch:
    inputs:
      mode:
        description: 'nav=update NAV | dump=dump all AMC fund names'
        required: false
        default: 'nav'

jobs:
  update-nav:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Dump all AMC fund names
        if: github.event.inputs.mode == 'dump'
        env:
          SEC_KEY_FACTSHEET: ${{ secrets.SEC_KEY_FACTSHEET }}
        run: node find-ssf-projids.js

      - name: Fetch NAV data
        if: github.event.inputs.mode != 'dump'
        env:
          SEC_KEY_DAILYINFO: ${{ secrets.SEC_KEY_DAILYINFO }}
        run: node fetch-nav.js

      - name: Commit nav-data.json
        if: github.event.inputs.mode != 'dump'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add nav-data.json
          git diff --staged --quiet || git commit -m "Update NAV data $(date +'%Y-%m-%d')"
          git push
